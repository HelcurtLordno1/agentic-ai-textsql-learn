name: Docs

on:
  workflow_dispatch:

jobs:
  links-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f README.md && test -f docs/architecture.md

