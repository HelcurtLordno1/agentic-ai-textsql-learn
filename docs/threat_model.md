# Threat model

Core boundaries include untrusted natural language, untrusted generated SQL, local database files,
resource exhaustion, and prompt/data leakage. Concrete policy tests begin in Phase 1.

